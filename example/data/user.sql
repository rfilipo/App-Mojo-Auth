BEGIN TRANSACTION;
CREATE TABLE IF NOT EXISTS `user` (
	`id`	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT UNIQUE,
	`name`	TEXT NOT NULL UNIQUE,
	`password`	TEXT NOT NULL UNIQUE
);
INSERT INTO `user` (id,name,password) VALUES (1,'monsenhor','12345');
COMMIT;
